分析数据一致性处理策略该如何实现(简单设计即可，不要给出具体代码)。
由于nebula、qdrant都没有真正的事务支持，故不需要事务支持。
可以给出多种方案并分析优缺点。
方案例如：
1.实现统一的批处理+额外实现简单的队列，分阶段执行解析。nebula或qdrant完成后往队列标记节点id，每个阶段只需要保证队列清空即可，避免每一步都要等待。
先执行完的可以开新的队列，但限制最大队列数。
2.你的方案，每一步都同步(qdrant与nebula的处理速度不一致，可能导致性能问题。理论上nebula处理较慢，但qdrant需要等待嵌入模型处理)
3.nebula不需要嵌入模型完成向量化，可以先开始。用队列标记nebula插入成功的实体节点的id，qdrant直接复用

对于1、3，parser模块、vector模块、graph模块都需要实现批处理