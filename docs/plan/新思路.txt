搜索可以指定前缀、路径(只允许项目内的相对路径)

服务需要同时为多个模块提供服务时可以设计独立的接口，从而降低耦合性

对于召回的片段使用正则匹配，能匹配的把优先级调高

查询添加更严格的约束，代替现有的意图识别(意图识别可以保留，在未使用过滤条件查询时依然可以发挥作用)
过滤条件添加到前端

通过语言服务器返回报错

静态检查增加对括号等标记的闭合问题的检查

图遍历检查循环依赖问题(需要设计多种判断逻辑)

不支持tree-sitter解析的文件可以考虑使用allminilm以换行为单位计算相似度，从而提供增强的纯文本文件分块

py微服务：
1.向量批处理
2.图搜索
3.重排

集成单轮对话用于文件搜索/分析。
用qwen2.5-7b或Qwen/Qwen2.5-Coder-7B-Instruct或THUDM/glm-4-9b-chat【硅基】或GLM-4.5-Flash【Zai】模型根据重排结果、相应文件内容对文件与问题的联系进行分析，输出摘要，
然后把重排结果、摘要汇总给ds-r1-0528-8b，给出最后的摘要。

向量查询：智能过滤/重排文档、测试文件，避免出现过多无关信息。
基于代码块来自的文件聚合召回片段

召回时支持过滤模式(针对文件，如docs，相对路径/文件名模式【如测试文件.test.ts/__tests__】)

token统计模块与api请求次数统计模块，限制RPS，TPS，RPM, TRM，以免超出平台速率限制

召回片段时拆分片段后计算相似度，再根据重排结果返回原先所在片段

txt, csv文件等可能用于存放数据集的需要先检查文件大小，以免增加系统负载

编写专门的检查脚本用于判定src\service\parser\core\normalization\adapters中的适配器与
src\service\parser\constants\queries目录中导出的查询规则
与src/service/graph/mapping/LanguageNodeTypes.ts中的图映射定义是否匹配


api：调用链完整性分析
给定json格式的调用链，根据图遍历结果验证

图查询：返回三元组