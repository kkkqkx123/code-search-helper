搜索可以指定前缀、路径(只允许项目内的相对路径)

服务需要同时为多个模块提供服务时可以设计独立的接口，从而降低耦合性

对于召回的片段使用正则匹配，能匹配的把优先级调高

查询添加更严格的约束，代替现有的意图识别(意图识别可以保留，在未使用过滤条件查询时依然可以发挥作用)
过滤条件添加到前端

通过语言服务器返回报错

静态检查增加对括号等标记的闭合问题的检查

图遍历检查循环依赖问题(需要设计多种判断逻辑)


py微服务：
1.向量批处理
2.图搜索
3.重排