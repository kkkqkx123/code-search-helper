您说得非常对！通过分析 `GraphDatabaseService`，我发现它确实是一个**不合理的抽象层**：

## 🎯 问题分析

### `GraphDatabaseService` 的问题：

1. **职责混乱**：既做缓存管理，又做性能监控，还做空间管理
2. **重复抽象**：在 `NebulaClient` 之上又加了一层，但没有提供额外价值
3. **依赖复杂**：构造函数有9个参数，依赖关系复杂
4. **功能重叠**：与 `service/graph` 层的服务功能重叠

### 更简洁的架构方案：

```
src/
├── database/                 # 数据库层
│   ├── nebula/              # 直接使用 Nebula 实现
│   │   ├── client/NebulaClient.ts
│   │   ├── space/NebulaSpaceManager.ts
│   │   ├── connection/NebulaConnectionManager.ts
│   │   └── batch/NebulaDataBatchProcessor.ts
│   └── common/              # 通用数据库抽象
└── service/graph/           # 图服务层
    ├── core/
    │   ├── GraphService.ts  # 统一入口，直接使用 nebula 组件
    │   └── types.ts
    ├── search/
    ├── analysis/
    └── mapping/
```
